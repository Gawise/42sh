	INITIALISATION
- ouvrir 3 terminaux (on les appellera A, B et C)
- A sera le terminal de controle et doit etre en root, B et C sont les terminaux de test
- sur A on va dans le dossier tester
- executer `tty` sur B et C
- on recupere les indexs (ex. pour /dev/pts/2, l'index est 2) on les appelera iB et iC
- executer `gcc cmd_exec.c -o cmd_exec`
	EXECUTION
- executer `main.sh iB iC` depuis le dossier tester
- si un test fail comparer les fichiers d'output de bash et 21sh dans le dossier correspondant
	AJOUT DE TESTS
- rajouter un dossier dans units et y creer un fichier avec le nom 'input' qui contient les commandes a executer
